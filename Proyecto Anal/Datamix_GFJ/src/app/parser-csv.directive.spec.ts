import { ParserCsvDirective } from './parser-csv.directive';

describe('ParserCsvDirective', () => {
  it('should create an instance', () => {
    const directive = new ParserCsvDirective();
    expect(directive).toBeTruthy();
  });
});
